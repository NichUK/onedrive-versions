{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile (tsc.cmd)",
      "type": "process",
      "command": "${workspaceFolder}\\node_modules\\.bin\\tsc.cmd",
      "args": [
        "-p",
        "./"
      ],
      "group": "build",
      "problemMatcher": "$tsc",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
